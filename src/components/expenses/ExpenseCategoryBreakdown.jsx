import {formatCurrency} from "../../utils/calculateSaleTotals";
function ExpenseCategoryBreakdown({items=[]}){if(items.length===0)return null;const max=Math.max(...items.map(item=>Number(item.total_amount||0)),1);return <section className="premium-surface rounded-xl p-5"><div className="mb-4"><h3 className="text-base font-extrabold text-slate-900">Spending by category</h3><p className="mt-1 text-xs text-slate-500">Breakdown for the current filters.</p></div><div className="grid gap-3 md:grid-cols-2">{items.map(item=><div key={item.id}><div className="mb-1.5 flex justify-between gap-4 text-xs"><span className="font-bold text-slate-700">{item.name}</span><span className="text-slate-500">{formatCurrency(item.total_amount)} � {item.expense_count}</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-500" style={{width:`${Math.max(3,Number(item.total_amount||0)/max*100)}%`}}/></div></div>)}</div></section>}
export default ExpenseCategoryBreakdown;
