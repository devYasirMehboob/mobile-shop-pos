import Icon from "../Icon";
const input="min-h-10 rounded-lg border border-slate-200 bg-white px-3 text-xs text-slate-700 outline-none focus:border-blue-400 focus:ring-4 focus:ring-blue-50";
function iso(date){return date.toISOString().slice(0,10)}
function ExpenseFilters({filters,search,categories,users,onSearch,onChange,onClear}){function preset(kind){const now=new Date(),from=new Date(now);if(kind==='week')from.setDate(now.getDate()-6);if(kind==='month')from.setDate(1);onChange({date_from:iso(from),date_to:iso(now),page:1});}return <section className="premium-surface rounded-xl p-4"><div className="flex flex-col gap-3 xl:flex-row"><label className="relative flex-1"><Icon name="search" className="absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-slate-400"/><input className={`${input} w-full pl-10`} value={search} onChange={e=>onSearch(e.target.value)} placeholder="Search title, reference or notes..."/></label><div className="flex flex-wrap gap-2"><button type="button" onClick={()=>preset('today')} className={input}>Today</button><button type="button" onClick={()=>preset('week')} className={input}>7 days</button><button type="button" onClick={()=>preset('month')} className={input}>This month</button><button type="button" onClick={onClear} className="px-3 text-xs font-bold text-red-600">Clear</button></div></div><div className="mt-3 grid gap-2 sm:grid-cols-2 xl:grid-cols-9"><input type="date" className={input} value={filters.date_from} onChange={e=>onChange({date_from:e.target.value,page:1})}/><input type="date" className={input} value={filters.date_to} onChange={e=>onChange({date_to:e.target.value,page:1})}/><select className={input} value={filters.category_id} onChange={e=>onChange({category_id:e.target.value,page:1})}><option value="">All categories</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select className={input} value={filters.added_by} onChange={e=>onChange({added_by:e.target.value,page:1})}><option value="">All users</option>{users.map(u=><option key={u.id} value={u.id}>{u.role}</option>)}</select><select className={input} value={filters.payment_method} onChange={e=>onChange({payment_method:e.target.value,page:1})}><option value="">All payments</option><option value="cash">Cash</option><option value="card">Card</option><option value="bank_transfer">Bank transfer</option><option value="mobile_wallet">Mobile wallet</option><option value="other">Other</option></select><select className={input} value={filters.status} onChange={e=>onChange({status:e.target.value,page:1})}><option value="active">Active</option><option value="voided">Voided</option><option value="all">All statuses</option></select><input type="number" min="0" step="0.01" className={input} value={filters.min_amount} onChange={e=>onChange({min_amount:e.target.value,page:1})} placeholder="Min amount"/><input type="number" min="0" step="0.01" className={input} value={filters.max_amount} onChange={e=>onChange({max_amount:e.target.value,page:1})} placeholder="Max amount"/><select className={input} value={`${filters.sort_by}:${filters.sort_direction}`} onChange={e=>{const [sort_by,sort_direction]=e.target.value.split(':');onChange({sort_by,sort_direction,page:1})}}><option value="expense_date:desc">Newest date</option><option value="expense_date:asc">Oldest date</option><option value="amount:desc">Highest amount</option><option value="amount:asc">Lowest amount</option><option value="title:asc">Title A-Z</option></select></div></section>}
export default ExpenseFilters;



