import {useEffect,useState} from "react";import Modal from "../Modal";import {formatCurrency} from "../../utils/calculateSaleTotals";
const today=()=>new Date().toISOString().slice(0,10);
function PurchasePaymentDialog({purchase,busy,onClose,onSubmit}){const[form,setForm]=useState({amount:"",payment_method:"cash",payment_date:today(),reference_number:"",notes:""});useEffect(()=>{if(purchase)setForm(f=>({...f,amount:purchase.balance_due}))},[purchase]);const set=(k,v)=>setForm(f=>({...f,[k]:v}));return <Modal isOpen={Boolean(purchase)} title="Record supplier payment" description={purchase?`${purchase.purchase_number} · Outstanding ${formatCurrency(purchase.balance_due)}`:""} onClose={onClose}><form onSubmit={e=>{e.preventDefault();onSubmit(form)}}><div className="grid gap-4 p-6 sm:grid-cols-2">{[["amount","Amount","number"],["payment_date","Payment date","date"],["reference_number","Reference","text"]].map(([k,l,t])=><label key={k} className="text-xs font-bold">{l}<input type={t} min={t==="number"?"0.01":undefined} step={t==="number"?"0.01":undefined} value={form[k]} onChange={e=>set(k,e.target.value)} className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3"/></label>)}<label className="text-xs font-bold">Method<select value={form.payment_method} onChange={e=>set("payment_method",e.target.value)} className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 px-3"><option value="cash">Cash</option><option value="bank_transfer">Bank transfer</option><option value="card">Card</option><option value="mobile_wallet">Mobile wallet</option><option value="other">Other</option></select></label><label className="text-xs font-bold sm:col-span-2">Notes<textarea value={form.notes} onChange={e=>set("notes",e.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 p-3"/></label></div><footer className="flex justify-end gap-2 border-t p-4"><button type="button" onClick={onClose} className="rounded-xl border px-4 py-2.5 text-xs font-bold">Cancel</button><button disabled={busy} className="rounded-xl bg-blue-600 px-5 py-2.5 text-xs font-bold text-white">Record payment</button></footer></form></Modal>}export default PurchasePaymentDialog;
