import {formatCurrency} from "../../utils/calculateSaleTotals";
const field="min-h-10 w-full rounded-lg border border-slate-200 px-3 text-right text-sm";
function PurchaseTotalsPanel({values,onChange,totals}){return <section className="premium-surface rounded-xl p-5"><h3 className="text-sm font-extrabold">Purchase totals and payment</h3><div className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{[["overall_discount","Overall discount"],["tax","Tax amount"],["shipping_amount","Shipping"],["other_charges","Other charges"],["amount_paid","Initial payment"]].map(([k,l])=><label key={k} className="text-xs font-bold text-slate-600">{l}<input type="number" min="0" step="0.01" value={values[k]} onChange={e=>onChange(k,e.target.value)} className={`mt-1.5 ${field}`}/></label>)}<label className="text-xs font-bold text-slate-600">Payment method<select value={values.payment_method} onChange={e=>onChange("payment_method",e.target.value)} className={`mt-1.5 ${field} text-left`}><option value="cash">Cash</option><option value="card">Card</option><option value="bank_transfer">Bank transfer</option><option value="mobile_wallet">Mobile wallet</option><option value="other">Other</option></select></label><label className="text-xs font-bold text-slate-600 sm:col-span-2">Payment reference<input value={values.payment_reference} onChange={e=>onChange("payment_reference",e.target.value)} className="mt-1.5 min-h-10 w-full rounded-lg border border-slate-200 px-3"/></label><label className="text-xs font-bold text-slate-600 sm:col-span-2 xl:col-span-3">Notes<textarea rows="2" value={values.notes} onChange={e=>onChange("notes",e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 p-3"/></label></div><dl className="mt-5 space-y-2 rounded-xl bg-slate-50 p-4 text-xs">{[["Items subtotal",totals.subtotal],["Overall discount",-totals.discount],["Tax",totals.tax],["Shipping and charges",totals.charges],["Grand total",totals.grand],["Balance due",Math.max(0,totals.grand-Number(values.amount_paid||0))]].map(([l,v])=><div key={l} className={`flex justify-between ${l==="Grand total"?"border-t pt-2 text-sm font-extrabold":""}`}><dt>{l}</dt><dd>{formatCurrency(v)}</dd></div>)}</dl></section>}
export default PurchaseTotalsPanel;
