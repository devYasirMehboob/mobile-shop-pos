import Icon from "../Icon";
import { formatCurrency } from "../../utils/calculateSaleTotals";
import SaleStatusBadge from "./SaleStatusBadge";

function SalesTable({ sales, permissions, onView, onReceipt, onAction }) {
  return <div className="overflow-x-auto"><table className="w-full min-w-[1450px] text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-400"><tr>{["Invoice","Date","Time","Cashier","Customer","Items","Subtotal","Discount","Tax","Grand total","Payment","Payment status","Sale status","Actions"].map((label)=><th key={label} className={`px-4 py-3 font-bold ${["Subtotal","Discount","Tax","Grand total"].includes(label)?"text-right":""}`}>{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{sales.map((sale)=>{const date=new Date(sale.created_at.replace(" ","T"));return <tr key={sale.id} className="hover:bg-slate-50/70"><td className="px-4 py-3.5 font-extrabold text-slate-900">{sale.invoice_number}</td><td className="px-4 py-3.5 text-slate-500">{date.toLocaleDateString("en-PK",{dateStyle:"medium"})}</td><td className="px-4 py-3.5 text-slate-500">{date.toLocaleTimeString("en-PK",{hour:"2-digit",minute:"2-digit"})}</td><td className="px-4 py-3.5 capitalize text-slate-600">{sale.cashier_name} · {sale.cashier_role}</td><td className="px-4 py-3.5"><strong className="block text-slate-700">{sale.customer_name||"Walk-in customer"}</strong>{sale.customer_phone&&<small className="text-slate-400">{sale.customer_phone}</small>}</td><td className="px-4 py-3.5 font-bold text-slate-600">{sale.item_count}</td><td className="px-4 py-3.5 text-right text-slate-600">{formatCurrency(sale.subtotal)}</td><td className="px-4 py-3.5 text-right text-slate-600">{formatCurrency(sale.discount_amount)}</td><td className="px-4 py-3.5 text-right text-slate-600">{formatCurrency(sale.tax_amount)}</td><td className="px-4 py-3.5 text-right font-extrabold text-slate-950">{formatCurrency(sale.grand_total)}</td><td className="px-4 py-3.5 capitalize text-slate-600">{sale.payment_method.replaceAll("_"," ")}</td><td className="px-4 py-3.5"><SaleStatusBadge status={sale.payment_status}/></td><td className="px-4 py-3.5"><SaleStatusBadge status={sale.status}/></td><td className="px-4 py-3.5"><div className="flex items-center gap-1"><button type="button" onClick={()=>onView(sale)} className="grid size-8 place-items-center rounded-lg text-slate-400 hover:bg-blue-50 hover:text-blue-600" title="View details"><Icon name="eye" className="size-4"/></button><button type="button" onClick={()=>onReceipt(sale)} className="grid size-8 place-items-center rounded-lg text-slate-400 hover:bg-blue-50 hover:text-blue-600" title="View receipt"><Icon name="print" className="size-4"/></button>{permissions.can_cancel&&sale.status==="completed"&&<button type="button" onClick={()=>onAction("cancel",sale)} className="rounded-lg px-2 py-1.5 text-[10px] font-bold text-red-600 hover:bg-red-50">Cancel</button>}{permissions.can_refund&&sale.status==="completed"&&<button type="button" onClick={()=>onAction("refund",sale)} className="rounded-lg px-2 py-1.5 text-[10px] font-bold text-amber-700 hover:bg-amber-50">Refund</button>}</div></td></tr>;})}</tbody></table></div>;
}
export default SalesTable;
