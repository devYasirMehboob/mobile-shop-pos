import {useEffect,useState}from"react";import Modal from"../Modal";
function ResetPasswordDialog({user,onClose,onSave}){const[form,setForm]=useState({password:"",password_confirmation:""});const[errors,setErrors]=useState({});const[saving,setSaving]=useState(false);useEffect(()=>{setForm({password:"",password_confirmation:""});setErrors({});},[user]);if(!user)return null;async function submit(event){event.preventDefault();setSaving(true);setErrors({});try{await onSave(form);}catch(error){setErrors(error.response?.data?.errors||{form:[error.response?.data?.message||"Unable to reset the password."]});}finally{setSaving(false);}}return <Modal isOpen title="Reset password" description={`Set a new unique password for ${user.name}. Their existing sessions will expire.`} onClose={saving?()=>{}:onClose} size="sm"><form onSubmit={submit}><div className="space-y-4 px-6 py-5">{errors.form&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{errors.form[0]}</p>}<Password label="New password" name="password" value={form.password} errors={errors.password} setForm={setForm}/><Password label="Confirm password" name="password_confirmation" value={form.password_confirmation} errors={errors.password_confirmation} setForm={setForm}/><p className="text-xs leading-5 text-slate-400">Passwords are never displayed or returned after saving.</p></div><footer className="flex justify-end gap-3 border-t border-slate-100 px-6 py-4"><button type="button" className="min-h-10 rounded-lg border border-slate-200 px-4 text-sm font-bold text-slate-600 hover:bg-slate-50 disabled:opacity-50" onClick={onClose}>Cancel</button><button type="submit" className="min-h-10 rounded-lg bg-blue-600 px-4 text-sm font-bold text-white hover:bg-blue-700 disabled:opacity-50" disabled={saving}>{saving?"Resetting...":"Reset password"}</button></footer></form></Modal>}
function Password({label,name,value,errors,setForm}){return <label className="block"><span className="mb-1.5 block text-xs font-bold text-slate-600">{label}</span><input className="min-h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm outline-none focus:border-blue-400 focus:ring-4 focus:ring-blue-50" type="password" name={name} autoComplete="new-password" value={value} onChange={(event)=>setForm((current)=>({...current,[name]:event.target.value}))}/>{errors&&<span className="mt-1 block text-xs text-red-600">{errors[0]}</span>}</label>}
export default ResetPasswordDialog;