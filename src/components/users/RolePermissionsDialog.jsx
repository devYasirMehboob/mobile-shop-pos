import{useEffect,useState}from"react";import Modal from"../Modal";import{getRolePermissions}from"../../api/usersApi";
function RolePermissionsDialog({isOpen,roles,permissions,onClose,onSave}){const[roleId,setRoleId]=useState("");const[selected,setSelected]=useState([]);const[loading,setLoading]=useState(false);const[saving,setSaving]=useState(false);const[error,setError]=useState("");const role=roles.find((item)=>String(item.id)===String(roleId));useEffect(()=>{if(isOpen&&!roleId&&roles.length)setRoleId(String(roles.find((item)=>item.slug==="cashier")?.id||roles[0].id));},[isOpen,roleId,roles]);useEffect(()=>{if(!isOpen||!roleId)return;let active=true;setLoading(true);getRolePermissions(roleId).then((data)=>{if(active)setSelected(data.permission_keys);}).catch((error)=>{if(active)setError(error.response?.data?.message||"Unable to load role permissions.");}).finally(()=>{if(active)setLoading(false);});return()=>{active=false};},[isOpen,roleId]);function toggle(key){setSelected((current)=>current.includes(key)?current.filter((item)=>item!==key):[...current,key]);}async function save(){setSaving(true);setError("");try{await onSave(roleId,selected);}catch(error){setError(error.response?.data?.message||"Unable to update role permissions.");}finally{setSaving(false);}}return <Modal isOpen={isOpen} title="Role permissions" description="Manage inherited access for each staff role." onClose={saving?()=>{}:onClose} size="lg"><div className="p-6"><label className="block max-w-xs"><span className="mb-1.5 block text-xs font-bold text-slate-600">Role</span><select className="min-h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm" value={roleId} onChange={(event)=>setRoleId(event.target.value)}>{roles.map((item)=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label>{role?.slug==="admin"&&<p className="mt-4 rounded-xl border border-blue-100 bg-blue-50 p-3 text-xs text-blue-700">Admin permissions are protected to prevent system lockout.</p>}{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-5 max-h-[50vh] space-y-5 overflow-y-auto pr-1">{loading?<p className="text-sm text-slate-500">Loading permissions...</p>:[...new Set(permissions.map((item)=>item.module))].map((module)=><section key={module}><h3 className="mb-2 text-[10px] font-extrabold uppercase tracking-[0.12em] text-slate-400">{module}</h3><div className="grid gap-2 sm:grid-cols-2">{permissions.filter((item)=>item.module===module).map((permission)=><label key={permission.id} className="flex gap-3 rounded-xl border border-slate-200 p-3"><input type="checkbox" className="mt-0.5 size-4 accent-blue-600" checked={selected.includes(permission.key)} disabled={role?.slug==="admin"} onChange={()=>toggle(permission.key)}/><span><strong className="block text-xs text-slate-800">{permission.name}</strong><span className="mt-1 block text-[10px] leading-4 text-slate-400">{permission.description}</span></span></label>)}</div></section>)}</div></div><footer className="flex justify-end gap-3 border-t border-slate-100 px-6 py-4"><button type="button" className="min-h-10 rounded-lg border border-slate-200 px-4 text-sm font-bold text-slate-600 hover:bg-slate-50" onClick={onClose}>Cancel</button><button type="button" className="min-h-10 rounded-lg bg-blue-600 px-4 text-sm font-bold text-white hover:bg-blue-700 disabled:opacity-50" disabled={saving||loading||role?.slug==="admin"} onClick={save}>{saving?"Saving...":"Update role"}</button></footer></Modal>}
export default RolePermissionsDialog;