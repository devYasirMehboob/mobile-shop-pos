import Modal from"../Modal";import StatusBadge from"../StatusBadge";
const date=(value)=>value?new Intl.DateTimeFormat("en-PK",{dateStyle:"medium",timeStyle:"short"}).format(new Date(value.replace(" ","T"))):"Never";
function UserDetailsModal({details,onClose}){if(!details)return null;const{user,recent_activity:activity=[]}=details;return <Modal isOpen title={user.name} description="Account details, effective access, and recent activity." onClose={onClose} size="lg"><div className="grid max-h-[72vh] gap-6 overflow-y-auto p-6 lg:grid-cols-[0.85fr_1.15fr]"><section><div className="rounded-xl border border-slate-200 p-5"><div className="flex items-center gap-4"><span className="grid size-12 place-items-center rounded-xl bg-blue-50 text-base font-extrabold text-blue-700">{user.name.charAt(0).toUpperCase()}</span><div><strong className="text-base font-extrabold text-slate-900">{user.name}</strong><p className="mt-1 text-xs capitalize text-slate-500">{user.role} account</p></div></div><dl className="mt-5 space-y-3 text-xs"><Row label="Status"><StatusBadge status={user.status}/></Row><Row label="Last login" value={date(user.last_login_at)}/><Row label="Created" value={date(user.created_at)}/><Row label="Updated" value={date(user.updated_at)}/></dl></div><div className="mt-4 rounded-xl border border-slate-200 p-5"><h3 className="text-xs font-extrabold uppercase tracking-[0.1em] text-slate-500">Effective permissions</h3><div className="mt-3 flex flex-wrap gap-1.5">{details.effective_permissions.map((key)=><span key={key} className="rounded-md bg-blue-50 px-2 py-1 text-[10px] font-bold text-blue-700">{key}</span>)}</div></div></section><section><h3 className="text-sm font-extrabold text-slate-900">Recent activity</h3>{activity.length===0?<p className="mt-4 rounded-xl bg-slate-50 p-5 text-sm text-slate-500">No recorded activity yet.</p>:<div className="mt-3 divide-y divide-slate-100 rounded-xl border border-slate-200">{activity.map((item)=><article key={item.id} className="px-4 py-3"><div className="flex justify-between gap-4"><strong className="text-xs text-slate-800">{item.description}</strong><time className="whitespace-nowrap text-[10px] text-slate-400">{date(item.created_at)}</time></div><p className="mt-1 text-[10px] text-slate-400">{item.actor_name||"System"} · {item.action}</p></article>)}</div>}</section></div></Modal>}
function Row({label,value,children}){return <div className="flex items-center justify-between gap-4"><dt className="text-slate-400">{label}</dt><dd className="font-bold text-slate-700">{children||value}</dd></div>}
export default UserDetailsModal;