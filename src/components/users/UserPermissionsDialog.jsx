import{useEffect,useMemo,useState}from"react";import Modal from"../Modal";import PermissionSelector from"./PermissionSelector";
function UserPermissionsDialog({details,permissions,onClose,onSave}){const[overrides,setOverrides]=useState({});const[saving,setSaving]=useState(false);const[error,setError]=useState("");useEffect(()=>{setOverrides(Object.fromEntries((details?.permission_overrides||[]).map((item)=>[item.key,item.effect])));setError("");},[details]);const inherited=details?.inherited_permissions||[];const disabled=details?.user.role==="admin";const effectiveCount=useMemo(()=>details?.effective_permissions?.length||0,[details]);if(!details)return null;function change(key,value){setOverrides((current)=>{const next={...current};if(value==="inherit")delete next[key];else next[key]=value;return next;});}async function submit(){setSaving(true);setError("");try{await onSave(overrides);}catch(error){setError(error.response?.data?.message||"Unable to update permissions.");}finally{setSaving(false);}}return <Modal isOpen title={`${details.user.name} permissions`} description={`${effectiveCount} effective permissions. Role access is inherited; overrides apply only to this user.`} onClose={saving?()=>{}:onClose} size="lg"><div className="max-h-[65vh] overflow-y-auto p-6">{disabled&&<p className="mb-4 rounded-xl border border-blue-100 bg-blue-50 p-3 text-xs text-blue-700">Admin access is protected and inherited from the Admin role.</p>}{error&&<p className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<PermissionSelector permissions={permissions} inherited={inherited} overrides={overrides} onChange={change} disabled={disabled}/></div><footer className="flex justify-end gap-3 border-t border-slate-100 px-6 py-4"><button type="button" className="min-h-10 rounded-lg border border-slate-200 px-4 text-sm font-bold text-slate-600 hover:bg-slate-50" onClick={onClose}>Cancel</button><button type="button" className="min-h-10 rounded-lg bg-blue-600 px-4 text-sm font-bold text-white hover:bg-blue-700 disabled:opacity-50" disabled={saving||disabled} onClick={submit}>{saving?"Saving...":"Save overrides"}</button></footer></Modal>}
export default UserPermissionsDialog;